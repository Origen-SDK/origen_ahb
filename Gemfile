source 'https://rubygems.org'

# Specify your gem's dependencies in origen_ahb.gemspec
gemspec

gem 'coveralls', require: false 

# Only these gems should be added here
if RUBY_VERSION >= '2.0.0'
  gem 'byebug', '~>3.5'
else
  gem 'debugger', '~>1.6'
end
